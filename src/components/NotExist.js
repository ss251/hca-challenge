import React from "react";

// re-direct route for an invalid url
const NotExist = () => (
  <div>
    <h2>Sorry, the page you are looking for does not exist.</h2>
  </div>
);

export default NotExist;
